import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const Category = () => {
  return (
    <ScrollView>
      {/* Category Card */}
      <Text>Category</Text>
    </ScrollView>
  )
}

export default Category